# Copy this file's contents into Streamlit Community Cloud's
# Settings -> Secrets for this app (NOT into a real secrets.toml file
# committed to GitHub - this .example file is safe to commit precisely
# because it has no real values in it).

db_password = "your-supabase-db-password-here"
dashboard_password = "choose-a-shared-password-for-colleagues-here"
